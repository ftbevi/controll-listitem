version: '3.4'

services:
  web:
    build:
      context: .
      dockerfile: ./dockerfile
    container_name: controllitem-server
    env_file:
      - ".env"
    volumes:
      - .:/server
    ports:
      - "6600:6600"
    depends_on:
      - db

  db:
    image: postgres:14-bullseye
    container_name: controllitem-db
    environment:
      POSTGRES_USER: "controllitem"
      POSTGRES_PASSWORD: "controllitem"
      POSTGRES_DB: "controllitem"
    ports:
      - "5498:5432"
    volumes:
      - ./data:/var/lib/postgresql/data/

volumes:
  data:
